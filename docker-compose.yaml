version: '3.6'
services:
  # nginx:
  #   image: nginx:latest
  #   restart: always
  #   ports:
  #     - "443:443"
  #   links:
  #     - wes-elixir
  #   volumes:
  #     - /docker/nginx/etc/ssl/:/etc/ssl/
  #     - /docker/nginx/etc/nginx/conf.d/:/etc/nginx/conf.d

  # wes-elixir:
  #   # image: epiph-meteor:latest
  #   build:
  #     context: .
  #     dockerfile: Dockerfile
  #   restart: always
  #   ports:
  #     - "7777:7777"
  #   # expose:
  #   #   - "7777"
  #   links:
  #     - rabbitmq
  #     - mongo
  #   command: python /WES-ELIXIR/wes_elixir/app.py
  #     # - cd /WES-ELIXIR/wes_elixir
  #     # - celery worker -A celery_worker -E --loglevel=info
  #   # environment:
  #     # MONGO_URL=mongodb://mongo:27017/wes-elixir-db

  rabbitmq:
    image: "rabbitmq:3.7.8"
    restart: always
    hostname: "rabbit1"
    ports:
      - "15672:15672"
    labels:
      NAME: "rabbitmq1"
    links:
      - mongo
    #volumes:
    #  - "./enabled_plugins:/etc/rabbitmq/enabled_plugins"

  mongo:
    image: mongo:4.1.3
    restart: always
    # ports:
    #   - "27017:27017"
    expose:
      - "27017"
    volumes:
      - /Users/gypas-adm/Documents/mongo/data/db:/data/db
      # - /home/cloud-user/mongo/data/db/:/data/db
    command: --auth
